# Swarmtally – Backend Dependencies
# Install with: pip install -r requirements.txt
#
# Versions are intentionally flexible to support Python 3.10 – 3.14.
# Pinning to exact versions caused build failures on Python 3.14+.

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# AI model inference (YOLOv8)
ultralytics>=8.2.0

# Image processing
# NOTE: use opencv-python-headless (no GUI) for server environments
opencv-python-headless>=4.8.0
Pillow>=10.4.0
numpy>=1.26.0

# File upload support (required by FastAPI for UploadFile)
python-multipart>=0.0.9

# Environment variables
python-dotenv>=1.0.1
